FROM python:3
ENV PYTHONUNBUFFERED 1
RUN mkdir /code
WORKDIR /code
COPY . /code/
RUN apt update && apt install vim cron -y
RUN pip install virtualenv \
     && virtualenv venv \
     && /bin/bash -c "source venv/bin/activate && pip install -r requirements.txt"
    
#COPY requirements.txt /code/
#RUN /bin/bash -c "source venv/bin/activate && pip install -r requirements.txt"
#RUN pip install -r requirements.txt

ENV  DJANGO_ENV DEV
ENV  DJANGO_DEBUG True
ENV  DJANGO_SECRET_KEY "WQWRWTWTRWE"
ENV  DJANGO_PROJECT_ROOT /code/
ENV AZURE_TENANT_ID=${AZURE_TENANT_ID}
ENV AZURE_CLIENT_ID=${AZURE_CLIENT_ID}
ENV AZURE_CLIENT_SECRET=${AZURE_CLIENT_SECRET}
COPY scripts/vmbucket-cron /etc/cron.d/vmbucket-cron
RUN chmod 0644 /etc/cron.d/vmbucket-cron \
    && touch /var/log/cron.log \
    && crontab /etc/cron.d/vmbucket-cron \
# CMD service cron start
#  CMD [" service cron start && ./scripts/startgunicorn.sh"]

CMD [ "./scripts/startgunicorn.sh" ]
EXPOSE 80 443 8000